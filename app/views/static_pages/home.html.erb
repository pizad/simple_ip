<% provide(:title, 'Home') %>

<% if signed_in? %>

	<%= render 'sessions/user_home_page' %>

<% else %>

<div class="center jumbotron">
	<h1>Welcome to simple IP<small>.beta</small></h1>

	<h3>Discover. Organize. Share.</h3>
	<%= link_to "Join Now!", '#', class: "btn btn-primary btn-lg" %>
	<%= link_to "Sign In", signin_path, class: "btn btn-default btn-lg" %>
</div>

<% end %>
